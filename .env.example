# URL of the Backend API
# For local development, this usually points to localhost.
# For production (Render), set this in the Dashboard to your live backend URL.
REACT_APP_BACKEND_URL=http://localhost:5000/api/v1